const jobs = document.querySelector('.jobs');
if(jobs){
    let togglerCollection = document.querySelector('.jobs-openner');
    togglerCollection.forEach((toggler)=>{
        toggler.addEventListener('click', ()=>{
            let parrent = toggler.closest('.jobs-item')
            if(parrent.classList.contains){
                
            }
        })
    })
}